name: Terraform - Lancer conteneur Docker

on:
  workflow_dispatch:

jobs:
  run-terraform:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3

      - name: Terraform Init & Apply
        working-directory: ./terraform
        run: |
          terraform init
          terraform apply -auto-approve

      - name: Conteneur lancé !
        run: echo "Ton conteneur Docker tourne dans GitHub Actions sur le port 8080"name: Terraform - Lancer conteneur Docker

on:
  workflow_dispatch:

jobs:
  run-terraform:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3

      - name: Terraform Init & Apply
        working-directory: ./terraform
        run: |
          terraform init
          terraform apply -auto-approve

      - name: Conteneur lancé !
        run: echo "Ton conteneur Docker tourne dans GitHub Actions sur le port 8080"